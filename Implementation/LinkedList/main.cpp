#include "LinkedList.h"
using namespace std;



class test{
    string name;

};
int main(){
    test t;
    string h;
    string *t1;
    cout<<sizeof(t1);

    // LinkedList<int>l;
    // l.append(1);
    // l.length();
    // l.print();
    // l.append(2);
    // l.length();
    // l.print();
    // l.append(3);
    // l.length();
    // l.print();
    // l.append(4);
    // l.length();
    // l.print();
    // l.append(5);
    // l.length();
    // l.print();
    // l.append(6);
    // l.length();
    // l.print();
    // l.append(7);
    // l.length();
    // l.print();
    // l.append(8);
    // l.length();
    // l.print();
    // l.append(9);
    // l.length();
    // l.print();
    

    // l.reverse();
    // l.print();

    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();
    // l.pop();
    // l.print();

    
}